<!--Form for creating a new place, made with help of GPT and class notes-->

<%= form_with model: @place, local: true do |form| %>
  <div>
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  
  <div>
    <%= form.submit "Save" %>
  </div>
<% end %>
<%= link_to 'Back', places_path %>
