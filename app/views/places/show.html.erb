<!--Display place name and link back to places list.
 Made with help of GPT based on my notes and assignment detail-->

<h2 style="color: blue;"><%= @place.name %></h2>


<% @place.entries.order(posted_on: :desc).each do |entry| %>
  <div>
    <h4><%= entry.title %></h4>
    <p><small style="font-style: italic;"><%= entry.posted_on.strftime("%B %d, %Y") %></small></p> <!--Worked with GPT to get formatting perfect here-->
    <p><%= entry.description %></p>
  </div>
<% end %>



<h4>Options:</h4>
<%= link_to 'Back to Places', places_path %>
<br>
<%= link_to 'Add New Entry', new_place_entry_path(@place) %>